"use strict";

var gId = function gId(child) {
    var parent = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : document;
    return parent.getElementById(child);
};

var qSel = function qSel(child) {
    var parent = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : document;
    return parent.querySelector(child);
};

var qSelAll = function qSelAll(child) {
    var parent = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : document;
    return parent.querySelectorAll(child);
};

var createEl = function createEl(tag, classname) {
    var new_elem = document.createElement(tag);
    classname ? new_elem.classList.add(classname) : null;
    return new_elem;
};

var getVal = function getVal(node) {
    return node.value !== undefined ? node.value.trim() : null;
};